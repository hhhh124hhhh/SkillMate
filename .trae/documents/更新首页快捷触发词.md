根据您的要求，我将更新 `EmptyState` 组件中的快捷按钮，精选以下 4 个高频场景，并直接使用您提供的自然语言触发词，这样用户点击后就能直接调用对应的 Skill。

### 🎯 选定的 4 个高频场景

| 图标 | 显示名称 | 触发词 (Action) | 对应 Skill |
| :--- | :--- | :--- | :--- |
| **PenTool** | **开始写作** | "帮我写一篇文章" | `ai-writer` / `wechat-workflow` |
| **Search** | **寻找选题** | "帮我找一些热门选题" | `topic-selector` |
| **Image** | **生成封面** | "为这篇文章生成封面" | `image-generation` |
| **Layout** | **文章排版** | "帮我排版这篇文章" | `smart-layout` |

*(注：原有的“运行脚本”对普通用户来说比较底层，替换为更直观的“文章排版”或“寻找选题”会更实用)*

### 🛠️ 修改计划

1.  **修改 `src/components/CoworkView.tsx`**：
    -   更新 `EmptyState` 组件中的按钮列表数据。
    -   引入新的图标（如需）。
    -   确保点击触发的文本是您指定的自然语言指令。

2.  **验证**：
    -   确认点击按钮后，输入框会自动填充对应的触发词并聚焦。

我将立即为您执行此更新。