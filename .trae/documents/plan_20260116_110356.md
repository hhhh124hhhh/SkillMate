## 修复配置和显示问题

### 需要修改的内容：

1. **更新 ConfigStore 默认值**（electron/config/ConfigStore.ts）
   - 将 `apiUrl` 默认值从 `https://open.bigmodel.cn/api/coding/paas` 改为 `https://open.bigmodel.cn/api/anthropic`

2. **清理旧配置数据**
   - 重启应用后，配置会使用新的默认值

3. **优化文字显示**（可选）
   - 检查 SettingsView.tsx 中输入框的 CSS 样式，确保文字清晰

### 预期效果：
- API URL 显示正确的值：`https://open.bigmodel.cn/api/anthropic`
- 文字显示清晰
- 配置值与用户预期一致