我已经完成了代码审查，并发现了几个关键的“定制化”和“敏感信息”风险点。为了将项目转化为可商业化的通用产品，我建议执行以下重构计划：

### 1. 🚨 安全与敏感信息清理（最高优先级）
目前代码中包含硬编码的 API 密钥和个人信息，必须立即清除：
- **移除硬编码 Key**：
  - `resources/skills/image-generation/scripts/doubao_image_gen.py`: 移除了硬编码的豆包 API Key。
  - `resources/skills/image-generation/config/config.yaml`: 包含真实的 API Key，需要重置为模板。
- **元数据脱敏**：
  - `package.json`: 移除个人邮箱，通用化 `description`。
  - `electron-builder.json5`: 移除个人的 GitHub 发布配置和邮箱。

### 2. 🔧 代码通用化与最佳实践重构
将特定的业务逻辑改为通用配置：
- **移除硬编码路径与名称**：
  - `doubao_image_gen.py`: 移除默认的 `project_name="智谱上市_GLM文章"` 和 `"公众号项目/"` 路径，改为参数传入或通用默认值。
- **字体适配优化**：
  - `cover_generator.py`: 移除对 `simhei.ttf` 的硬编码依赖，增加字体自动回退机制（优先使用打包字体 -> 系统字体 -> 默认字体），防止在不同系统上报错。
- **配置标准化**：
  - 确保所有 Python 脚本统一从环境变量或标准配置文件读取配置，而不是各自维护一套逻辑。

### 3. 📦 封装与交付准备
- **配置模板化**：确保所有配置文件都有对应的 `.example` 或 `.template` 文件。
- **文档更新**：更新 `README.md`，说明如何配置环境变量和 API Key，而不是让用户去修改代码。

### 执行步骤
1.  **清理**：立即修改上述包含敏感信息的文件。
2.  **重构**：修改 Python 脚本，增强通用性和健壮性。
3.  **配置**：更新 `package.json` 和构建配置。

确认后，我将开始执行这些清理和重构工作。