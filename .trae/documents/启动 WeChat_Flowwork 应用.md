## 启动 SkillMate 应用

### 步骤：
1. **安装依赖** - 运行 `npm install` 安装所有项目依赖
2. **检查环境变量** - 验证 `.env` 文件中的 API Keys 配置
3. **启动开发服务器** - 运行 `npm run dev` 启动应用

### 注意事项：
- 首次安装可能需要几分钟
- 确保 Python 环境可用（某些技能依赖 Python）
- 如需完整功能，建议配置 ZHIPU_API_KEY 和 DOUBAO_API_KEY