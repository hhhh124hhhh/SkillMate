# 高级功能指南

**文档导航**：
- [🏠 返回 README](../README.md) - 5分钟快速上手
- [📖 快速开始指南](./getting-started.md) - 标准版教程
- [📖 完整文档列表](../README.md#-文档)

---

本指南面向希望深入了解 SkillMate 高级功能的用户和开发者。

## 目录

- [技能开发](#技能开发)
- [MCP 集成深入](#mcp-集成深入)
- [性能优化](#性能优化)
- [安全机制](#安全机制)
- [高级技巧](#高级技巧)
- [故障排查](#故障排查)
- [最佳实践](#最佳实践)

---

## 技能开发

### 创建自定义技能

技能（Skill）是 SkillMate 的核心扩展机制，允许你为 AI 添加特定的能力和知识。

#### 基本结构

每个技能是一个目录，包含 `SKILL.md` 文件：

```
resources/skills/my-skill/
├── SKILL.md          # 技能定义文件（必需）
├── examples/         # 可选：示例文件
└── references/       # 可选：参考文档
```

#### SKILL.md 格式

```yaml
---
name: my-skill
description: 我的自定义技能描述
input_schema:
  type: object
  properties:
    query:
      type: string
      description: 用户输入的内容
  required: [query]
---

这里是技能的具体指令内容...

## 使用场景

- 场景 1：...
- 场景 2：...

## 操作步骤

1. 第一步...
2. 第二步...

## 注意事项

- 注意事项 1
- 注意事项 2
```

#### 技能参数和输入验证

使用 `input_schema` 定义技能接受的参数：

```yaml
---
name: code-review
description: 代码审查技能
input_schema:
  type: object
  properties:
    file_path:
      type: string
      description: 要审查的文件路径
    focus_areas:
      type: array
      items:
        type: string
      description: 关注领域（性能、安全、可读性等）
    strict_mode:
      type: boolean
      description: 是否启用严格模式
  required: [file_path]
---
```

#### 技能最佳实践

**1. 命名规范**
- 使用小写字母和连字符：`my-awesome-skill`
- 避免使用特殊字符和空格
- 保持简洁但描述性

**2. 描述优化**
- 清晰说明技能的功能
- 突出适用场景
- 限制在 50 字以内

**3. 指令编写**
- 使用明确的步骤结构
- 提供具体示例
- 包含边界情况处理

**4. 错误处理**
```yaml
## 错误处理

如果遇到以下情况：
- 文件不存在：提示用户检查路径
- 格式不正确：提供格式示例
- 权限不足：引导用户授权目录
```

#### 示例：从零创建一个技能

让我们创建一个"项目文档生成器"技能：

```bash
# 1. 创建技能目录
mkdir -p resources/skills/project-doc-gen

# 2. 创建 SKILL.md
cat > resources/skills/project-doc-gen/SKILL.md << 'EOF'
---
name: project-doc-gen
description: 自动生成项目文档（README、API文档等）
input_schema:
  type: object
  properties:
    project_type:
      type: string
      description: 项目类型（web、desktop、library等）
    doc_type:
      type: string
      description: 文档类型（readme、api、guide）
    style:
      type: string
      description: 文档风格（minimal、detailed、enterprise）
  required: [project_type, doc_type]
---

# 项目文档生成器

你是一个专业的技术文档生成助手。根据用户提供的信息，生成高质量的项目文档。

## 分析项目

1. 读取项目结构
2. 分析 package.json / 配置文件
3. 识别技术栈和依赖
4. 理解项目架构

## 生成文档

根据文档类型（{{doc_type}}）：

### README.md
- 项目简介
- 安装步骤
- 使用说明
- API 概览
- 贡献指南

### API 文档
- 接口列表
- 请求/响应格式
- 错误码说明
- 使用示例

### 使用指南
- 快速开始
- 常见场景
- 最佳实践
- 故障排查

## 文档风格

- **minimal**：简洁版，适合小项目
- **detailed**：详细版，包含完整说明
- **enterprise**：企业级，包含架构图、流程图等

## 输出格式

使用 Markdown 格式，包含：
- 清晰的章节结构
- 代码示例
- 链接引用
- 必要的图表（ASCII）

## 质量检查

生成后检查：
- ✅ 链接是否有效
- ✅ 代码示例是否正确
- ✅ 步骤是否完整
- ✅ 术语是否一致

请开始分析项目并生成文档。
EOF

# 3. 重启应用加载技能
```

---

## MCP 集成深入

MCP (Model Context Protocol) 是 SkillMate 的核心扩展协议，允许集成外部工具和服务。

### MCP 协议原理

MCP 定义了 AI 助手与外部服务之间的标准化通信接口：

```
┌─────────────┐         ┌─────────────┐         ┌──────────────┐
│   AI Agent  │ ──IPC──▶│ MCP Client  │ ──Stdio─▶│ MCP Server   │
│  (主进程)    │         │  (客户端)    │         │  (外部服务)   │
└─────────────┘         └─────────────┘         └──────────────┘
                               │
                               └─── HTTP ──▶ Remote API
```

### 配置自定义 MCP 服务器

#### 方式一：stdio 类型（本地进程）

在设置 → "MCP 配置"中添加：

```json
{
  "mcpServers": {
    "my-local-server": {
      "name": "my-local-server",
      "description": "我的本地 MCP 服务器",
      "type": "stdio",
      "command": "node",
      "args": ["/path/to/server.js"],
      "env": {
        "API_KEY": "your-api-key"
      }
    }
  }
}
```

#### 方式二：streamableHttp 类型（远程 API）

```json
{
  "mcpServers": {
    "my-remote-api": {
      "name": "my-remote-api",
      "description": "远程 API 服务",
      "type": "streamableHttp",
      "baseUrl": "https://api.example.com/mcp",
      "headers": {
        "Authorization": "Bearer ${API_KEY}",
        "Content-Type": "application/json"
      }
    }
  }
}
```

### stdio vs streamableHttp

| 特性 | stdio | streamableHttp |
|------|-------|----------------|
| **通信方式** | 标准输入/输出 | HTTP 请求 |
| **部署位置** | 本地进程 | 远程服务器 |
| **性能** | 更快（本地） | 取决于网络 |
| **适用场景** | Python脚本、Node.js工具 | 云服务、第三方API |
| **配置复杂度** | 需要安装运行时 | 只需 URL |

### 调试 MCP 连接

#### 1. 查看日志

开发模式下，MCP 通信日志会显示在终端：

```
[MCP] Connecting to server: my-local-server
[MCP] Server started with PID: 12345
[MCP] Initialized successfully
[MCP] Available tools: tool1, tool2, tool3
```

#### 2. 测试连接

在设置 → "MCP 配置"中，点击"测试连接"按钮：

```
✅ 连接成功
可用工具: 3 个
⏱️ 响应时间: 45ms
```

#### 3. 常见问题排查

**问题：连接失败**
```bash
# 检查命令是否可执行
which node  # 或 python, python3

# 检查文件权限
ls -l /path/to/server.js

# 手动测试
node /path/to/server.js
```

**问题：工具不显示**
- 检查服务器是否正确实现 `listTools` 方法
- 查看 MCP 日志确认工具列表
- 验证工具名称格式

**问题：调用超时**
- 增加 `timeout` 配置（默认 30 秒）
- 检查网络连接（远程 API）
- 优化服务器性能

### 常见 MCP 服务器推荐

#### 文件系统增强

```json
{
  "filesystem": {
    "type": "stdio",
    "command": "npx",
    "args": ["-y", "@modelcontextprotocol/server-filesystem", "/allowed/path"]
  }
}
```

#### 网页抓取

```json
{
  "fetch": {
    "type": "stdio",
    "command": "npx",
    "args": ["-y", "@modelcontextprotocol/server-fetch"]
  }
}
```

#### 数据库查询

```json
{
  "postgres": {
    "type": "stdio",
    "command": "npx",
    "args": ["-y", "@modelcontextprotocol/server-postgres", "postgresql://..."]
  }
}
```

#### GitHub 集成

```json
{
  "github": {
    "type": "stdio",
    "command": "npx",
    "args": ["-y", "@modelcontextprotocol/server-github"],
    "env": {
      "GITHUB_TOKEN": "your-github-token"
    }
  }
}
```

---

## 性能优化

### 技能懒加载机制

SkillMate v2.0 引入了技能懒加载，显著提升启动速度：

**工作原理**：
```
启动应用
  ↓
加载核心技能（10个必需技能）
  ↓
用户触发需求
  ↓
动态加载相关技能
  ↓
缓存已加载技能
```

**优化效果**：
- 启动时间：从 3.5s 降至 0.8s（**77% 提升**）
- 内存占用：减少 40%（按需加载）
- 技能可用性：无影响（用户无感知）

### 缓存策略

#### 1. 技能元数据缓存

```typescript
// electron/agent/skills/SkillManager.ts
const CACHE_TTL = 5 * 60 * 1000; // 5分钟

private cache = new Map<string, {
  data: SkillMetadata;
  timestamp: number;
}>();

getSkill(name: string) {
  const cached = this.cache.get(name);
  if (cached && Date.now() - cached.timestamp < CACHE_TTL) {
    return cached.data;
  }

  const skill = this.loadSkill(name);
  this.cache.set(name, { data: skill, timestamp: Date.now() });
  return skill;
}
```

#### 2. 技能内容缓存

- **热技能**：频繁使用的技能常驻内存
- **冷技能**：不常用技能按需加载，自动卸载
- **容量限制**：最多缓存 50 个技能

#### 3. 清理缓存

在设置 → "性能"中可以：

```
┌─────────────────────────────┐
│  缓存管理                    │
│  ────────────────────────   │
│  当前缓存：35 个技能         │
│  占用内存：12.5 MB           │
│                              │
│  [清除所有缓存]              │
│  [重置统计]                  │
└─────────────────────────────┘
```

### 内存管理

#### 自动内存优化

SkillMate 会自动监控内存使用：

```typescript
// 每 30 秒检查一次
setInterval(() => {
  const usage = process.memoryUsage();
  if (usage.heapUsed > MEMORY_THRESHOLD) {
    // 触发清理
    this.skillManager.unloadColdSkills();
    this.mcpClient.cleanupIdleConnections();
  }
}, 30000);
```

#### 手动优化建议

1. **定期重启**：长时间使用后重启应用
2. **限制技能数**：禁用不常用的技能
3. **清理会话**：删除不需要的历史会话

### 启动速度优化

#### 优化前后对比

| 版本 | 启动时间 | 内存占用 |
|------|----------|----------|
| v1.0 | 3.5s     | 180 MB   |
| v2.0 | 0.8s     | 110 MB   |
| **提升** | **77%** | **39%** |

#### 优化技术

1. **并行加载**
   ```typescript
   // 同时加载多个独立模块
   await Promise.all([
     loadSkills(),
     loadMcpServers(),
     loadUserConfig()
   ]);
   ```

2. **代码分割**
   ```typescript
   // 动态导入大型模块
   const HeavyModule = await import('./heavy-module.js');
   ```

3. **Tree Shaking**
   ```javascript
   // vite.config.ts
   export default {
     build: {
       rollupOptions: {
         output: {
           manualChunks: {
             'vendor': ['react', 'react-dom'],
             'mcp': ['@modelcontextprotocol/sdk']
           }
         }
       }
     }
   };
   ```

---

## 安全机制

### 权限系统详解

SkillMate 采用细粒度的权限管理系统，确保 AI 操作安全可控。

#### 权限确认机制

```
用户请求 AI 操作
  ↓
AI 分析操作类型
  ↓
┌─────────────────────────┐
│ 判断是否需要确认         │
│  • 读取文件：无需确认    │
│  • 写入文件：需要确认    │
│  • 删除文件：需要确认    │
│  • 执行命令：需要确认    │
└─────────────────────────┘
  ↓
弹出确认对话框
  ↓
用户选择：允许 / 拒绝
  ↓
执行或取消操作
```

#### 权限级别

| 级别 | 操作 | 确认方式 |
|------|------|----------|
| **安全** | 读取已授权文件 | 无需确认 |
| **中等** | 写入文件 | 单次确认 |
| **危险** | 删除文件 | 二次确认 |
| **高危** | 执行命令 | 二次确认 + 显示命令 |

#### 授权目录管理

**在设置 → "权限管理"中**：

```
┌──────────────────────────────┐
│  已授权目录                   │
│  ─────────────────────────   │
│  📂 D:\Projects              │
│     └─ 读取 ✅ 写入 ✅        │
│                              │
│  📂 C:\Users\Name\Documents  │
│     └─ 读取 ✅ 写入 ⚠️        │
│                              │
│  [+ 添加授权目录]             │
└──────────────────────────────┘
```

**权限类型**：
- **只读**：AI 只能读取，不能修改
- **读写**：AI 可以读取和修改文件
- **完全**：AI 可以读取、修改、删除（谨慎使用）

### 沙箱隔离

#### Context Isolation

SkillMate 启用了 Electron 的 Context Isolation：

```typescript
// electron/main.ts
const mainWindow = new BrowserWindow({
  webPreferences: {
    nodeIntegration: false,      // 禁用 Node 集成
    contextIsolation: true,      // 启用上下文隔离
    sandbox: true,               // 启用沙箱
    preload: path.join(__dirname, 'preload.js')
  }
});
```

**好处**：
- 渲染进程无法直接访问 Node.js API
- 所有 IPC 通信通过预加载脚本
- 防止恶意代码注入

#### CSP（内容安全策略）

```typescript
// electron/main.ts
session.defaultSession.webRequest.onHeadersReceived((details, callback) => {
  callback({
    responseHeaders: {
      ...details.responseHeaders,
      'Content-Security-Policy': [
        "default-src 'self';" +
        "script-src 'self';" +
        "connect-src 'self' https://open.bigmodel.cn;" +
        "img-src 'self' data: https:;"
      ]
    }
  });
});
```

### 安全最佳实践

#### 1. 授权原则

```
最小权限原则：
├─ 只授权必需的目录
├─ 优先使用"只读"权限
└─ 避免授权系统目录
```

#### 2. 敏感操作

对于危险操作，SkillMate 会：

```
删除文件操作：
┌──────────────────────────────┐
│  ⚠️ 警告                      │
│                              │
│  AI 即将删除以下文件：         │
│  • D:\Projects\old-code.js   │
│                              │
│  此操作不可撤销！             │
│                              │
│  [取消]  [确认删除]           │
└──────────────────────────────┘
```

#### 3. 审计日志

所有敏感操作都会记录到日志：

```log
[2024-01-15 14:30:22] SECURITY: File write operation
  Path: D:\Projects\test.js
  User: allowed
  AI: claude-opus-4.5

[2024-01-15 14:31:05] SECURITY: Command execution
  Command: npm install
  User: allowed
  AI: claude-opus-4.5
```

查看日志：设置 → "日志" → "安全日志"

---

## 高级技巧

### 自定义快捷按钮

快捷按钮可以让你快速执行常用任务。

#### 创建自定义按钮

1. 打开设置 → "快捷操作"
2. 点击"添加新按钮"
3. 配置按钮属性：

```
┌──────────────────────────────┐
│  按钮配置                     │
│  ─────────────────────────   │
│  图标：💡                     │
│  标签：代码审查               │
│  颜色：蓝色                   │
│                              │
│  提示词模板：                  │
│  ┌────────────────────────┐ │
│  │请帮我审查当前选中的代码  │ │
│  │关注以下方面：            │ │
│  │1. 代码质量              │ │
│  │2. 性能优化              │ │
│  │3. 安全问题              │ │
│  └────────────────────────┘ │
│                              │
│  [保存] [取消]                │
└──────────────────────────────┘
```

#### 可用图标（17个）

💻 💡 🔍 🎨 📝 🚀 🔧 📊
⚡ 🎯 📦 🌟 🔥 📌 ✅ ❌ ⭐

#### 可用颜色（6种）

- 蓝色（默认）
- 绿色（成功）
- 红色（危险）
- 黄色（警告）
- 紫色（高级）
- 灰色（中性）

### 悬浮球配置

悬浮球让你随时呼出 AI 助手。

#### 基本设置

设置 → "悬浮球"：

```
┌──────────────────────────────┐
│  悬浮球设置                   │
│  ─────────────────────────   │
│  ☑ 启用悬浮球                │
│                              │
│  快捷键：                     │
│  [ Alt + Space ]             │
│                              │
│  位置：                       │
│  (●) 右下角                  │
│  (  ) 右上角                 │
│  (  ) 左下角                 │
│  (  ) 左上角                 │
│                              │
│  透明度：                     │
│  ━━━━━●━━━━ 80%              │
└──────────────────────────────┘
```

#### 高级选项

- **自动隐藏**：失去焦点时自动隐藏
- **置顶显示**：始终在最上层
- **拖拽移动**：可拖动到任意位置
- **大小调整**：支持 3 种尺寸

### 命令面板定制

命令面板（`Ctrl+Shift+P`）可以快速访问所有功能。

#### 自定义命令

在技能中定义命令：

```yaml
---
name: my-skill
description: 我的技能
commands:
  - name: quick-action
    description: 快速操作
    template: |
      使用 my-skill 执行快速操作...
---
```

#### 命令别名

为常用命令设置别名：

```json
{
  "aliases": {
    "review": "code-review",
    "gen": "code-generator",
    "test": "test-runner"
  }
}
```

### 工作流自动化

#### 创建工作流

工作流是一系列操作的组合：

```json
{
  "name": "新功能开发流程",
  "steps": [
    {
      "action": "skill",
      "skill": "plan-feature",
      "params": {
        "feature": "{{user_input}}"
      }
    },
    {
      "action": "mcp",
      "server": "github",
      "tool": "create-branch"
    },
    {
      "action": "skill",
      "skill": "code-generator",
      "params": {
        "plan": "{{previous_result}}"
      }
    }
  ]
}
```

#### 触发工作流

1. 通过命令面板：`/workflow 新功能开发流程`
2. 通过快捷按钮：配置工作流按钮
3. 通过 API：`ipcRenderer.invoke('run-workflow', name)`

---

## 故障排查

### 常见问题诊断

#### 问题 1：应用启动失败

**症状**：运行 `npm start` 后窗口不显示

**诊断步骤**：

```bash
# 1. 检查端口占用
netstat -ano | findstr :5173

# 2. 检查 Vite 服务
curl http://localhost:5173

# 3. 查看详细日志
DEBUG=* npm start
```

**常见原因**：
- 端口 5173 被占用
- Node.js 版本过低
- 依赖安装不完整

**解决方案**：
```bash
# 杀掉占用进程
npx kill-port 5173

# 重新安装依赖
rm -rf node_modules package-lock.json
npm install

# 升级 Node.js
nvm install 20
nvm use 20
```

#### 问题 2：AI 不响应

**症状**：发送消息后 AI 一直不回复

**诊断步骤**：

1. **检查 API Key**
   ```
   设置 → API 配置 → 测试连接
   ```

2. **查看网络日志**
   ```
   开发者工具 → Network → 过滤: api.bigmodel.cn
   ```

3. **检查余额**
   ```
   访问: https://open.bigmodel.cn/console/finance
   ```

**常见原因**：
- API Key 无效或过期
- 网络连接问题
- API 余额不足
- 请求频率超限

#### 问题 3：技能加载失败

**症状**：某些技能不显示或调用失败

**诊断步骤**：

```bash
# 1. 检查技能文件
ls -la resources/skills/problem-skill/

# 2. 验证 YAML 格式
npx yaml-validator resources/skills/problem-skill/SKILL.md

# 3. 查看加载日志
# 终端输出中搜索 "Error loading skill"
```

**常见原因**：
- YAML frontmatter 格式错误
- 技能名称重复
- 缺少必需字段（name, description）

**解决方案**：
```yaml
# 正确格式
---
name: my-skill           # 必需，英文，连字符
description: 描述        # 必需
input_schema: {...}      # 可选
---

技能内容...
```

#### 问题 4：MCP 服务器连接失败

**症状**：MCP 工具无法使用

**诊断步骤**：

```bash
# 1. 测试 stdio 服务器
node /path/to/server.js

# 2. 测试 HTTP 服务器
curl https://api.example.com/mcp

# 3. 查看 MCP 日志
# 终端输出中搜索 [MCP]
```

**常见原因**：
- 服务器路径错误
- 运行时环境未安装（Node.js/Python）
- 网络连接问题（远程 API）
- 配置格式错误

### 日志分析

#### 日志级别

```typescript
// electron/main.ts
app.setAppLogsPath(path.join(app.getPath('userData'), 'logs'));

const log = electronLog.create('main');
log.transports.file.level = 'debug';  // debug, info, warn, error
```

#### 查看日志

**方式一：应用内**
```
设置 → 日志 → 查看日志
```

**方式二：文件系统**
```
%APPDATA%\SkillMate\logs\main.log
```

**方式三：终端输出**（开发模式）
```
npm start  # 所有日志会显示在终端
```

#### 日志格式

```log
[2024-01-15 14:30:22.123] [INFO] [main] Application started
[2024-01-15 14:30:23.456] [DEBUG] [skills] Loading skill: my-skill
[2024-01-15 14:30:24.789] [WARN] [mcp] MCP server timeout: fetch
[2024-01-15 14:30:25.012] [ERROR] [ipc] API request failed: 401
```

### 性能分析

#### 性能监控

打开开发者工具 → Performance：

```
1. 点击"开始录制"
2. 执行操作
3. 点击"停止"
4. 分析结果：
   - CPU 使用率
   - 内存占用
   - 调用栈
   - 渲染性能
```

#### 性能指标

| 指标 | 正常值 | 警告值 | 危险值 |
|------|--------|--------|--------|
| 启动时间 | < 2s | 2-5s | > 5s |
| 内存占用 | < 200 MB | 200-500 MB | > 500 MB |
| CPU 使用 | < 10% | 10-30% | > 30% |
| 技能加载 | < 100 ms | 100-500 ms | > 500 ms |

#### 优化建议

```bash
# 1. 清理缓存
设置 → 性能 → 清除所有缓存

# 2. 禁用不必要的技能
设置 → 技能管理 → 禁用

# 3. 减少会话历史
设置 → 数据管理 → 清理历史会话

# 4. 重启应用
# 定期重启可以释放内存
```

---

## 最佳实践

### 项目组织建议

#### 目录结构

```
SkillMate-Workspace/
├── projects/              # 项目目录（授权）
│   ├── project-a/
│   └── project-b/
├── knowledge/             # 知识库（只读授权）
│   ├── docs/
│   └── references/
├── temp/                  # 临时文件（不授权）
└── skills-custom/         # 自定义技能
    ├── my-skill-1/
    └── my-skill-2/
```

#### 授权策略

```
├─ projects/      → 读写权限（开发）
├─ knowledge/     → 只读权限（参考）
└─ temp/          → 不授权（临时）
```

### 技能库管理

#### 技能分类

```
resources/skills/
├── development/       # 开发相关
│   ├── code-review/
│   ├── test-gen/
│   └── refactor/
├── writing/          # 写作相关
│   ├── blog-writer/
│   ├── doc-gen/
│   └── translator/
├── productivity/     # 效率工具
│   ├── task-planner/
│   └── meeting-sum/
└── media/            # 媒体处理
    ├── image-gen/
    └── video-edit/
```

#### 技能版本管理

```yaml
---
name: my-skill
version: "2.0.0"
skill_api: "1.0"
update_date: "2024-01-15"
changelog: |
  - 2.0.0: 添加新功能X
  - 1.5.0: 修复问题Y
  - 1.0.0: 初始版本
---
```

### 备份和恢复

#### 导出配置

```bash
# 方式一：应用内导出
设置 → 数据管理 → 导出配置

# 方式二：手动备份
# 复制以下目录：
%APPDATA%\SkillMate\
├── config.json         # 应用配置
├── skills/             # 自定义技能
├── mcp-config.json     # MCP 配置
└── sessions/           # 会话历史
```

#### 导入配置

```bash
# 方式一：应用内导入
设置 → 数据管理 → 导入配置

# 方式二：手动恢复
# 停止应用 → 替换文件 → 启动应用
```

### 团队协作

#### 共享技能

```bash
# 1. 创建技能仓库
git init skills-repo
cd skills-repo

# 2. 添加技能
cp -r resources/skills/my-skill/* .

# 3. 提交和共享
git add .
git commit -m "Add my-skill"
git push origin main

# 4. 团队成员安装
git clone https://github.com/team/skills-repo.git
cp -r skills-repo/* resources/skills/
```

#### 共享配置

```bash
# 1. 导出配置（不包含敏感信息）
设置 → 数据管理 → 导出配置（排除 API Key）

# 2. 提交到团队仓库
git add config-template.json
git commit -m "Add team config template"

# 3. 团队成员使用
# 导入模板 → 填入自己的 API Key
```

#### 协作最佳实践

1. **技能标准化**
   - 统一命名规范
   - 统一文件结构
   - 添加使用文档

2. **版本控制**
   - 使用 Git 管理技能
   - 记录变更日志
   - 标记稳定版本

3. **文档共享**
   - 维护团队知识库
   - 记录最佳实践
   - 分享使用案例

---

## 继续学习

恭喜你掌握了 SkillMate 的高级功能！

**下一步**：
- 📖 [贡献指南](../README.md#-贡献) - 参与项目开发
- 🐛 [报告问题](https://github.com/hhhh124hhhh/SkillMate/issues) - 帮助改进项目
- 💡 [功能建议](https://github.com/hhhh124hhhh/SkillMate/discussions) - 分享你的想法

---

<p align="center">
  <b>成为 SkillMate 专家，探索 AI Agent 的无限可能</b> 🚀
</p>

---

**文档导航**：
- [🏠 返回 README](../README.md) - 5分钟快速上手
- [📖 快速开始指南](./getting-started.md) - 标准版教程
- [📖 完整文档列表](../README.md#-文档) - 所有文档
